# Copy this file to .env.local and fill in the values from your Supabase
# project: Project Settings -> API. Use the "anon public" key — never the
# "service_role" key, which must stay server-side only.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
